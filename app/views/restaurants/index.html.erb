<p style="color: green"><%= notice %></p>

<h1>Restaurants</h1>

<div id="restaurants">
  <% @restaurants.each do |restaurant| %>
    <%# <%= render restaurants %>
    <p>
      <%= link_to "#{restaurant.name}", restaurant %>
    </p>
  <% end %>
</div>
